<!DOCTYPE html>
<html lang="${request.locale_name}" i18n:domain="occams_studies">

  <tal:version define="pkg import:occams_studies">
  <!-- Application: ${pkg.__name__} -->
  <!-- Version:     ${pkg.__version__} -->
  </tal:version>

  <head>
    <meta charset="utf-8">

    <title>${request.registry.settings['app.org.title']|'OCCAMS'} - Studies</title>

    <meta name="mobile-web-app-capable" content="yes">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

    <link tal:repeat="url request.webassets_env['studies-css'].urls()" href="${url}" rel="stylesheet" type="text/css" />

    <!-- Shortcut icons -->
    <link
        rel="shortcut icon"
        href="${request.static_path('occams_studies:static/images/favicon.ico')}"/>
    <link rel="shortcut icon" sizes="1024x1024" href="${request.static_path('occams_studies:static/images/favicon-highres.png')}">
    <link rel="shortcut icon" sizes="128x128" href="${request.static_path('occams_studies:static/images/favicon.png')}">
    <link rel="apple-touch-icon" sizes="128x128" href="${request.static_path('occams_studies:static/images/favicon.png')}">
    <link rel="apple-touch-icon-precomposed" sizes="128x128" href="${request.static_path('occams_studies:static/images/favicon.png')}">
  </head>

  <body>

    <div id="wrap">
      <!--! Keep the app navbar separate from the content container -->
      <nav
          class="navbar navbar-default navbar-static-top"
          role="navigation"
          i18n:domain="occams_studies"
          tal:define="user request.user|nothing;">

        <div class="container">

          <div class="navbar-header">
            <button
                type="button"
                class="navbar-toggle"
                data-toggle="collapse"
                data-target=".navbar-collapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand"
                href="${request.route_path('studies.main')}">OCCAMS Studies</a>
          </div> <!--/.navbar-header -->

          <div class="collapse navbar-collapse">

            <ul class="nav navbar-nav"
                tal:define="section request.current_route_path(_query={})">

              <li tal:define="
                    factory import:occams_studies.models.StudyFactory;
                    studies factory(request);
                    url request.route_path('studies.main');
                    current_study request.matchdict['study']|nothing;
                    active request.matched_route.name.startswith('stud');"
                  tal:attributes="class python:'active' if active else ''"
                  tal:condition="request.has_permission('view', studies)"
                  id="studies-menu"
                  class="dropdown">
                <a  id="studies-dropdown"
                    href="${url}"
                    data-toggle="dropdown">Studies <span class="caret"></span></a>
                <ul class="dropdown-menu"
                    role="menu"
                    aria-labelledby="studies-dropdown">
                  <li role="presentation"
                      tal:repeat="study available_studies"
                      class="${'active' if current_study == study.name else ''}">
                    <!--! The study will inevitably be updated, so it's good to
                          also come back and update the menu via jquery (since
                          this part is not bound to knockout).
                    -->
                    <a role="menuitem"
                        class="js-study-option"
                        data-id="${study.id}"
                        tabindex="-1"
                        href="${request.route_path('studies.study', study=study.name)}">${study.title}</a>
                  </li>
                </ul>
              </li> <!-- /#studies-menu -->

              <li tal:define="
                    factory import:occams_studies.models.ExportFactory;
                    exports factory(request);
                    url request.route_path('studies.exports');
                    active request.matched_route.name.startswith('studies.export');"
                  tal:attributes="class python:'active' if active else ''"
                  tal:condition="request.has_permission('view', exports)"
                  id="exports-menu"
                  class="dropdown">

                <a  id="exports-dropdown"
                    href="${url}"
                    data-toggle="dropdown"
                    i18n:translate="">Exports <span class="caret"></span></a>
                <ul class="dropdown-menu"
                    role="menu"
                    aria-labelledby="exports-dropdown">
                  <li role="presentation"
                      tal:define="route string:studies.exports"
                      class="${'active' if request.matched_route.name == route else ''}">
                    <a role="menuitem" tabindex="-1" href="${request.route_path(route)}" i18n:translate="">Overview</a>
                  </li>
                  <li role="presentation"
                      tal:define="route string:studies.exports_checkout"
                      class="${'active' if request.matched_route.name == route else ''}">
                    <a role="menuitem" tabindex="-1" href="${request.route_path(route)}" i18n:translate="">Create an Export</a>
                  </li>
                  <li role="presentation"
                      tal:define="route string:studies.exports_status"
                      class="${'active' if request.matched_route.name == route else ''}">
                    <a role="menuitem" tabindex="-1" href="${request.route_path(route)}" i18n:translate="">My Exports</a>
                  </li>
                  <li role="presentation"
                      tal:define="route string:studies.exports_codebook"
                      class="${'active' if request.matched_route.name == route else ''}">
                    <a role="menuitem" tabindex="-1" href="${request.route_path(route)}" i18n:translate="">Codebook</a>
                  </li>
                  <li role="presentation"
                      tal:define="route string:studies.exports_faq"
                      class="${'active' if request.matched_route.name == route else ''}">
                    <a role="menuitem" tabindex="-1" href="${request.route_path(route)}" i18n:translate="">FAQ</a>
                  </li>
                </ul>
              </li> <!-- /#exports-menu -->
            </ul>

            <!--! Search box: do not display if already in the search view -->
            <form class="navbar-form navbar-left" role="search"
                method="GET"
                action="${request.route_path('studies.patients')}"
                tal:define="
                  factory import:occams_studies.models.PatientFactory;
                  patients factory(request)"
                tal:condition="
                  request.has_permission('view', patients)
                    and request.matched_route.name != 'studies.patients'">
              <div class="form-group">
                <label class="sr-only" for="search">Search</label>
                <div class="input-group">
                  <input
                      type="search"
                      class="form-control"
                      name="query"
                      placeholder="Search for Patient"
                      i18n:attributes="placeholder">
                  <span class="input-group-btn">
                    <button type="submit" class="btn btn-default"
                      ><span class="glyphicon glyphicon-search"></span></button>
                  </span>
                </div>
              </div>
            </form>

            <ul class="nav navbar-nav navbar-right"
                tal:define="user request.environ['repoze.who.identity']['properties']|nothing;"
                  tal:condition="user">
              <li><p class="navbar-text">${user['first_name']}</p></li>
              <li>
                <a tal:condition="request.has_permission('admin', context)"
                    href="${request.route_path('studies.settings')}"
                    rel="tooltip"
                    data-placement="bottom"
                    data-container="body"
                    data-animation="false"
                    title="Configure Application"
                  ><span class="fa fa-cog"></span></a>
              </li>
              <li class="dropdown"
                  tal:condition="request.apps">
                <a href="#apps"
                  role="button"
                  class="dropdown-toggle"
                  rel="tooltip"
                  data-toggle="dropdown"
                  data-placement="bottom"
                  data-container="body"
                  data-animation="false"
                  title="Services"
                  ><span class="glyphicon glyphicon-th"></span><span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu" aria-labelledby="Apps Menu">
                  <tal:suites repeat="suite request.apps">
                    <li role="presentation" class="dropdown-header">
                      ${suite['title']}
                    </li>
                    <tal:apps repeat="app suite['apps']">
                      <li role="presentation">
                        <a href="${app['url']}" role="menuitem">${app['title']}</a>
                      </li>
                    </tal:apps>
                    <li role="presentation" class="divider"
                        tal:condition="not:repeat.suite.end"></li>
                  </tal:suites>
                </ul>
              </li>
              <li>
                <a href="${request.route_path('accounts.logout')}"
                    rel="tooltip"
                    data-placement="bottom"
                    data-container="body"
                    data-animation="false"
                    title="Log Out"
                  ><span class="glyphicon glyphicon-log-out"></span></a>
              </li>
            </ul>

          </div><!--/.nav-collapse -->

        </div>
      </nav>
      <main
          id="view-${request.matched_route.name}"
          class="container"
          data-view-name="${request.matched_route.name}"
          data-view-path="${request.current_route_path()}"
          data-socket-io-resource="${request.route_path('socket.io', remaining='').strip('/')}">

        <tal:alerts define="types python:['danger', 'warning', 'success', 'info']" repeat="type types">
          <div class="alert alert-${type}" tal:define="alerts python:request.session.pop_flash(type)" tal:repeat="alert alerts">${alert}</div>
        </tal:alerts>

        <metal:content-slot define-slot="content-slot" />
      </main>
    </div> <!-- /#wrap -->

    <footer id="footer">
      <div class="container">
        <p class="text-muted credit">
          <span i18n:translate="">Developed by the</span>
          <a href="https://bitcore.ucsd.edu" i18n:translate="">YoungLabs</a>.
        </p>
      </div>
    </footer>

    <!--! Load scripts at the end so we don't block rendering -->
    <script
        tal:repeat="url request.webassets_env['studies-js'].urls()"
        src="${url}"
        type="text/javascript"></script>

    <metal:content-slot define-slot="javascript-extras-slot">
      <!--! Slot for injecting extra javascript into the applciation (e.g. ko initialization) -->
    </metal:content-slot>

  </body>
</html>

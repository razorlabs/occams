;; Bare-bones configuration for functional testing
[app:main]
use = egg:occams.clinical

app.org.name = myorg
app.org.title = MY ORGANIZATION

app.export.dir = /tmp
app.export.user = celery@localhost

;app.db.url = sqlite:///%(here)s/test_clinical.db
app.db.url = sqlite://

pid.package = occams.roster
;pid.db.url = sqlite:///%(here)s/test_roster.db
pid.db.url = sqlite://

; redis (regular access)
redis.url = redis://localhost/9

; redis (browser session)
redis.sessions.secret = sekrit

webassets.base_dir = %(here)s/../src/occams/clinical/static
webassets.base_url = /static
webassets.debug = False

who.config_file = %(here)s/who.ini
who.identifier_id =

suite.file =

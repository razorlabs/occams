<textarea
  tal:define="rows rows|field.widget.rows;
              cols cols|field.widget.cols;
              css_class css_class|field.widget.css_class;
              oid oid|field.oid;
              name name|field.name;"
  tal:attributes="rows rows;
                  cols cols;"
  id="${oid}"
  name="${name}"
  class="form-control ${css_class}">${cstruct}</textarea>

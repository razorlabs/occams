<nav tal:define="section section|request.current_route_path()">
  <ul class="nav nav-header">
    <li tal:define="url request.current_route_path(_route_name='data_list')"
        tal:attributes="class python:'active' if section == url else ''">
      <a href="${url}">All Data</a>
    </li>
    <li tal:define="url request.current_route_path(_route_name='data_export')"
        tal:attributes="class python:'active' if section == url else ''">
      <a href="${url}">Download</a>
    </li>
  </ul>
</nav>


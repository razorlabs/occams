<nav
    class="navbar navbar-inverse navbar-fixed-top"
    role="navigation">
  <div class="container">

    <div class="navbar-header">
      <button
          type="button"
          class="navbar-toggle"
          data-toggle="collapse"
          data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand"
          href="${request.route_path('home')}">OCCAMS | Clinical</a>
    </div> <!--/.navbar-header -->

    <div class="collapse navbar-collapse">

      <ul class="nav navbar-nav">
        <li class="active"><a href="${request.route_path('home')}">Home</a></li>
        <li tal:condition="request.has_permission('site_view')">
          <a href="${request.route_path('site_list')}">Sites</a>
        </li>
        <li tal:condition="request.has_permission('study_view')">
          <a href="${request.route_path('study_list')}">Studies</a>
        </li>
        <li tal:condition="request.has_permission('fia_view')">
          <a href="${request.route_path('export_list')}">Exports</a>
        </li>
      </ul>

      <ul class="nav navbar-nav navbar-right"
          tal:define="user request.user|nothing"
          tal:condition="user">
        <li><a href="#" class="disabled"
          ><span class="glyphicon glyphicon-user"></span> ${user.fullname}</a></li>
        <li class="dropdown">
          <a href="#"
            role="button"
            class="dropdown-toggle"
            data-toggle="dropdown"
            ><span class="glyphicon glyphicon-th"></span> Apps<span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu" aria-labelledby="Apps Menu">
            <li role="presentation" class="dropdown-header">OCCAMS</li>
            <li role="presentation"><a href="#" role="menuitem">Form</a></li>
            <li role="presentation"><a href="#" role="menuitem">Lab</a></li>
            <li role="presentation"><a href="#" role="menuitem">Roster</a></li>
            <li role="presentation" class="divider"></li>
            <li role="presentation" class="dropdown-header">CNICS</li>
            <li role="presentation"><a href="#" role="menuitem">HIV Sequence</a></li>
          </ul>
        <li>
          <a href="${request.route_path('account_logout')}
            "><span class="glyphicon glyphicon-log-out"></span> Log Out</a>
        </li>
      </ul>

    </div><!--/.nav-collapse -->

  </div>
</nav>
